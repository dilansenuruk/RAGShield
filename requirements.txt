# loads your .env file (Groq key) into environment variables
python-dotenv

# the web framework for the backend
fastapi

# the server that actually runs the FastAPI app
uvicorn

# creates unique IDs
uuid

# main vector database
chromadb

# library splits long pieces of text into smaller, manageable chunks
langchain-text-splitters

# this library used to read, write, split, merge, crop, and transform PDF files
pypdf

# used to convert sentences, paragraphs, or entire documents into dense vector embeddings
sentence-transformers
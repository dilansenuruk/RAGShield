# loads your .env file (Groq key) into environment variables
python-dotenv

# the web framework for the backend
fastapi

# the server that actually runs the FastAPI app
uvicorn

# search files -> ex: data/**/*.txt
glob

# reading environment variables, creating file paths, checking directories
os

# creates unique IDs
uuid

# main vector database
chromadb